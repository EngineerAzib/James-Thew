@model James_Thew.context.Racipies_james

@{
    Layout = null;
    ViewBag.Title = "Details_Recp";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details_Recp</title>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        h2 {
            background-color: #007bff;
            color: #fff;
            padding: 10px;
            text-align: center;
        }

        .container {
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin: 20px auto;
            max-width: 600px;
        }

        dl.dl-horizontal dt {
            font-weight: bold;
        }

        dl.dl-horizontal dd {
            margin-left: 0;
            margin-bottom: 10px;
        }

        p {
            text-align: center;
            margin-top: 20px;
        }

        .back-link {
            text-decoration: none;
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border-radius: 5px;
        }

            .back-link:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <h2>Recipe Details</h2>
    <div class="container">
        <h4>Racipies_james</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>@Html.DisplayNameFor(model => model.Title)</dt>
            <dd>@Html.DisplayFor(model => model.Title)</dd>

            <dt>@Html.DisplayNameFor(model => model.Ingredient)</dt>
            <dd>@Html.DisplayFor(model => model.Ingredient)</dd>

            <dt>@Html.DisplayNameFor(model => model.CookingProcedure)</dt>
            <dd>@Html.DisplayFor(model => model.CookingProcedure)</dd>

        </dl>
    </div>
    <p>
        @Html.ActionLink("Back to List", "Index", null, new { @class = "back-link" })
    </p>
</body>
</html>
